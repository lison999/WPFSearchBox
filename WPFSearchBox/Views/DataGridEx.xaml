<Window x:Class="WpfApp1.Views.DataGridEx"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp1.Views"
        mc:Ignorable="d"
        Title="DataGridEx" Height="450" Width="800">
    <Grid>

        <DataGrid>
            <DataGrid.Columns>
                <DataGridTemplateColumn></DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>


        <!--<ItemsControl ItemsSource="{Binding 我想暴露出去的集合}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding Title}" />
                    <ItemsControl ItemsSource="{Binding 我想暴露出去的集合.第二层集合名称}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Button Content="{我想暴露出去的集合.第二层集合名称.Name}"/>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>-->
                      
    </Grid>
</Window>
